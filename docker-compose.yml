services:
  backend:
    build: ./backend
    container_name: hali-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always
    command: node index.js
    volumes:
      - ./backend/uploads:/app/uploads
    
    

  frontend:
    build:
      context: ./client
      args:
        VITE_API_URL: http://158.180.32.235:5000
    container_name: hali-frontend
    ports:
      - "5173:80"
    restart: always